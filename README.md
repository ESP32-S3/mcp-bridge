# mcp-url-bridge
Powershell (.ps1) script that creates a Quick Tunnel with cloudfare and uses supergateway to expose MCP tools from the configured in-script mcp file path and hosts it on the configured localhost.
